# Copy to .env.local before running `npx next dev`.
# In Docker these are injected via docker-compose.yml; this file is for local dev.

BACKEND_URL=http://localhost:8000
GO_SSE_URL=http://localhost:8080
