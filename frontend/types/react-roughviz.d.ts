declare module "react-roughviz" {
  // We declare these as 'any' so TS stops complaining,
  // while still allowing you to pass your custom props.
  export const Area: any;
  export const Bar: any;
  export const Line: any;
}
